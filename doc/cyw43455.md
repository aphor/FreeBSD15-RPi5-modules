# CYW43455 SDIO WiFi — Reference

Cross-referenced summary of the SDIO host-driver-relevant behavior of the
Cypress/Infineon CYW43455 (FullMAC 802.11ac + BT5.0 combo), drawn from:

- **DS** = `_reference/infineon-cyw43455-datasheet-en.pdf`
- **L**  = Linux brcmfmac at `../raspbian_linux.git/drivers/net/wireless/broadcom/brcm80211/brcmfmac/{sdio,chip,bcdc}.c`
- **FB** = FreeBSD brcmfmac port at `../freebsd-brcmfmac.git/src/{sdio,sdpcm,fwil}.c` and `docs/03-sdio-reference.md`
- **T**  = facts established (or refuted) by our own testing in this driver
  (cited as **T:** with the symptom that established them)

**Driver name note:** historical dmesg excerpts in this document show
`cyw434550:` — the `driver_t` name has since been renamed from `"cyw43455"` to
`"cyw"` (review item 6) so the device now appears as `cyw0:` in dmesg.

The chip identifies as `chip_id=0x4345 rev=6`. Linux's chip table records
this combination as `BRCM_CC_43454_CHIP_ID` despite the part marking
"CYW43455" — branded as 43455 by Cypress/Infineon, the silicon is the
same as Broadcom's 43454 reference. SR detection at chip.c:1426 keys on
that ID.

---

## 1. Chip architecture (DS §1.2; L chip.c)

Three on-die clients share a BCMA backplane:

| Core            | Role                            | BCMA core ID |
|-----------------|---------------------------------|--------------|
| ARM Cortex-M4 (`CR4`) | Host CPU running the FullMAC firmware | `0x83e` |
| D11             | 802.11 MAC engine                | `0x812` |
| SDIO Device     | SDIO target controller, F1/F2 endpoint | `0x829` |
| ChipCommon      | PMU, OTP, GCI, sr_control        | `0x800` |
| ATCM/BTCM       | Tightly-coupled RAM for CR4      | (not BCMA cores) |

The CR4 runs out of TCM, which on this chip is **800 KB** total spread
across 8 banks (read from EROM at boot; see our `cyw_sdio.c` dmesg
"CR4 bank N" trace). The firmware blob `brcmfmac43455-sdio.bin`
(~609 KB) is downloaded to TCM base `0x00198000`.

NVRAM (~1.7 KB) is written to the **top** of TCM at
`ram_base + ram_size − nvram_len − 4`, with a trailing inverted-length
trailer that the firmware uses to find the blob. NVRAM contents pin
board-specific calibration; on Pi5 this is the `cyw43455-rpi.txt`
shipped with the firmware package.

---

## 2. SDIO function layout (DS §2; L sdio.c:200-)

| Func | Block | Role |
|------|-------|------|
| F0   | n/a   | Standard CCCR — `sdiob` programs this |
| F1   | 64    | Backplane access (registers, RAM, EROM) — synchronous CMD52/CMD53 |
| F2   | 512   | WLAN packet DMA (SDPCM frames in both directions) |
| F3   | —     | BT signaling on some SKUs; **not present** for our use |

Block sizes are configured via CCCR FBR registers (`sdiob` does this).
We use **F1 BS=64** the whole time and **F2 BS=64 during boot, then
F2 BS=512** after firmware handshake — Linux does the same; **T:**
trying to leave F2 at 64 post-boot causes BS-mismatch transfer
failures.

### F1 fixed-address FIFO at 0x10000

Backplane accesses use a sliding 32 KB window. The host writes a
"BAR0" address into F1 registers `0x1000A/B/C` (`SBADDRLOW/MID/HIGH`)
and then issues CMD52/CMD53 against F1 offsets `0x00000..0x07FFF` to
hit `BAR0 + offset`. SBADDR auto-clears on each access only for some
registers (`SBSDIO_SB_OFT_ADDR_*`); writes through it do **not**.

### F2 FIFO addresses

F2 transfers always target a fixed address (`addr_inc=0` in CMD53).
The conventional F2 FIFO base used by brcmfmac is `0x8000` (relative
to F2). Reads always return one full SDPCM frame; writes always
deliver one frame.

---

## 3. Backplane registers we actually touch (L sdio.c:62-; FB sdio.c)

All offsets are **F1** byte offsets unless otherwise noted.

| Symbol                        | Offset   | Notes |
|-------------------------------|----------|-------|
| `SBSDIO_FUNC1_SBADDRLOW`      | `0x1000A`| BAR0[15:8] |
| `SBSDIO_FUNC1_SBADDRMID`      | `0x1000B`| BAR0[23:16] |
| `SBSDIO_FUNC1_SBADDRHIGH`     | `0x1000C`| BAR0[31:24] |
| `SBSDIO_FUNC1_CHIPCLKCSR`     | `0x1000E`| Clock control (ALP_REQ, HT_REQ, ALP_AVAIL, HT_AVAIL, FORCE_HT) |
| `SBSDIO_FUNC1_SLEEPCSR`       | `0x1000E` *(typo? — actually `0x18000650` BP-relative)* | KSO + DEVON bits.  **T:** dunn dmesg shows reads of this at `0x1000e` succeeding — **the SDIOD-core SLEEPCSR is mirrored into the F1 register space at this same offset**. Linux brcmfmac calls this `SBSDIO_FUNC1_SLEEPCSR`, addressing it via F1 CMD52. |
| `SD_REG_INTSTATUS`            | `sdio_core + 0x020` | RW1C interrupt status (BP read32) |
| `SD_REG_HOSTINTMASK`          | `sdio_core + 0x024` | Bits that drive the SDIO IRQ line |
| `SD_REG_TOSBMAILBOX`          | `sdio_core + 0x040` | Host → device doorbell (`SMB_*` constants) |
| `SD_REG_TOSBMAILBOXDATA`      | `sdio_core + 0x048` | Host → device data word |
| `SD_REG_TOHOSTMAILBOXDATA`    | `sdio_core + 0x04C` | Device → host data word |

`sdio_core` is the BCMA base address of the SDIO Device core. Our
EROM walk prints this as `0x18004000`.

---

## 4. Clock state machine (DS §3.4; L sdio.c brcmf_sdio_clkctl)

`CHIPCLKCSR` (F1 reg `0x1000E`) carries:

```
   bit 7   FORCE_HT        force HT (160 MHz backplane) on
   bit 6   FORCE_ALP       force ALP (~26 MHz backplane) on
   bit 4   ALP_REQ         host requests ALP available
   bit 3   HT_REQ          host requests HT available
   bit 1   ALP_AVAIL       cleared/set by chip
   bit 0   HT_AVAIL        cleared/set by chip
```

State transitions exercised:

```
  POR ──────────────► [ALP off,HT off] ──set ALP_REQ──► poll ALP_AVAIL
                                                           │
                                                set HT_REQ │  (firmware download)
                                                           ▼
                                                  [ALP avail,HT pending]
                                                           │
                                                  poll HT_AVAIL
                                                           ▼
                                            [ALP+HT avail; FORCE_HT set]
                                                           │
                                         (firmware running)│
                                                           ▼
                                            ── detach: CHIPCLKCSR=0 ──► [off]
```

Backplane register access (BAR0 sliding-window reads) requires at
least **ALP** to be available. CR4 needs **HT**. We hold HT for the
whole firmware-download phase by setting `FORCE_HT` and clearing it
only just before passing control to the running firmware (which
manages its own clock requests).

**T:** During detach we re-enable ALP (via the same enable_clock path)
before issuing `cyw_arm_halt`'s backplane writes — without ALP the
SBADDR writes return EIO and the halt sequence is a no-op, which is
how `kldunload` + `kldload` started failing before the fix in commit
`708f92a`.

---

## 5. KSO — Keep SDIO On (L sdio.c:725- ; DS §4.3 "Sleep Modes")

The chip implements a backplane-sleep mechanism. While **KSO** is
cleared, the entire backplane is gated off; **every F1 register access
returns EIO**. F2 packet traffic still flows because that path has its
own clock.

`SLEEPCSR` (F1 reg `0x1000E`, same offset as `CHIPCLKCSR` — the
register decode is context-dependent on the SDIOD core; Linux defines
both at the same offset and they read back as distinct values
depending on which sub-decode the access hits):

```
   bit 1   KSO         Keep SDIO on
   bit 0   DEVON       Device on (read-only, reflects PMU state)
```

Wake sequence (Linux brcmf_sdio_kso_control, sdio.c:725):

1. Write `KSO=1` once — this is the wake-up command; **may not be
   ACKed** because the AOS (Always-On Subsystem) hasn't routed the
   write yet. The write is fire-and-forget.
2. Poll the register for `KSO=1 && DEVON=1`. The first **few** reads
   may return EIO or stale data; sleep `KSO_WAIT_US` (=50 µs) between
   tries, up to `PMU_MAX_TRANSITION_DLY/50 ≈ 110` tries.
3. Once `DEVON` reads back, the backplane is up.

Sleep sequence is just `KSO=0`, fire-and-forget. **No readback** —
the very write that clears KSO is the last thing the host can do
until something wakes it.

**Our driver** sets KSO once at probe (`KSO init: SLEEPCSR=0x03 [KSO
set]` in dmesg) and never sleeps the chip in normal operation
(Milestone 1/2 has no runtime sleep watchdog). Linux drives KSO from
its bus-state machine and the (firmware-side) "DSTANDBY" indications.

---

## 6. SDPCM framing (DS §2.4; L sdio.c, sdpcm.c)

Every F2 frame, in both directions, is wrapped in a fixed-layout
**SDPCM header** followed by optional protocol payload. Our struct
matches Linux's:

```
  offset 0    u16 len           total frame length, LE
  offset 2    u16 len_inv       ~len (used as a checksum)
  offset 4    u8  seq           8-bit sequence number, wraps
  offset 5    u8  chan_flags    low nibble = channel
  offset 6    u8  next_len      next-frame length hint (we ignore)
  offset 7    u8  data_offset   bytes from frame start to payload
  offset 8    u8  flow_ctl      device→host flow control bits
  offset 9    u8  credit        device→host: max seq the host may send
  offset 10   u8  reserved[2]
```

**Channels**:

| chan | name  | payload protocol             |
|------|-------|------------------------------|
| 0    | CTRL  | BCDC-encapsulated IOCTL/IOVAR |
| 1    | EVENT | BCDC event packets (assoc, scan results, …) |
| 2    | DATA  | 802.3 Ethernet frames |
| 3    | GLOM  | super-frame containing multiple sub-frames (we do not implement) |

**Credit flow control**: the chip tells the host its current "max
acceptable seq" via `hdr->credit`. The host tracks `tx_seq` (next seq
to send) and may only send while `(uint8_t)(rx_max - tx_seq) > 0`.
The seq number wraps modulo 256; the subtraction is intentionally
performed as `uint8_t` so it works across wrap. Seeding `rx_max = 4`
at attach is mandatory — without it the first TX never gets a credit
because the firmware never sends a starting credit update.

### BCDC (Broadcom DongleCmd) — CTRL/EVENT payload

```
  offset 0    u32 cmd     command code (e.g. WLC_GET_VAR=262)
  offset 4    u32 len     payload length following the BCDC header
  offset 8    u32 flags   bit 0: SET/GET, bits 16..31: transaction id
  offset 12   u32 status  device→host completion code
```

For an IOVAR, payload is the IOVAR name (NUL-terminated) followed by
SET data (or zero-filled for GET — the firmware fills it).

---

## 7. Interrupts and the INTSTATUS quirk (L sdio.c:200-, 2820-)

The SDIOD core advertises events to the host through `INTSTATUS`,
which is RW1C (write-1-to-clear). The host masks events with
`HOSTINTMASK` — bits set there drive the physical SDIO IRQ line.

### Bit layout (sdio.c:210-232)

| Bit   | Name              | Meaning |
|-------|-------------------|---------|
| 4     | `I_HMB_SW0` / `I_HMB_FC_STATE`  | flow-control state |
| 5     | `I_HMB_SW1` / `I_HMB_FC_CHANGE` | flow-control state changed |
| 6     | `I_HMB_SW2` / **`I_HMB_FRAME_IND`** | **firmware has a frame for host** |
| 7     | `I_HMB_SW3` / `I_HMB_HOST_INT`  | misc / host-defined |
| 4–7   | `I_HMB_SW_MASK`   | mask of all four mailbox bits |
| 23    | `I_XMTDATA_AVAIL` | "bits in fifo" — see quirk below |
| 29    | `I_CHIPACTIVE`    | chip transitioned doze→active |

Linux's `HOSTINTMASK = I_HMB_SW_MASK | I_CHIPACTIVE` (sdio.c:793).
**It does not include `I_XMTDATA_AVAIL`.** The Linux RX dispatcher
also explicitly `intstatus |= I_HMB_FRAME_IND;` in places where it
needs the next pass to read F2 — that bit is treated as
"there might be a frame", not a hardware guarantee.

### The reload INTSTATUS quirk (T:)

Cold-boot INTSTATUS right after fw handshake:
`0x20c000c0` = `I_HMB_HOST_INT | I_HMB_FRAME_IND | I_XMTDATA_AVAIL |
0x00400000 | I_CHIPACTIVE`.

After `cyw_arm_halt` + reload, INTSTATUS **at the moment the handshake
completes** (i.e. once `FWREADY` is set — see §7 TOHOSTMAILBOXDATA
below) varies: observed values include `0xa0400080` (`I_HMB_SW0` bit
set — within `I_HMB_SW_MASK`) and `0x20c000c0` (same as cold boot).
Either way, at least one `I_HMB_SW_MASK` or `I_XMTDATA_AVAIL` bit is
set by the time the firmware is ready.

The gate in `cyw_sdpcm_recv_one`:
```
if ((intstatus & (I_HMB_SW_MASK | I_XMTDATA_AVAIL)) == 0)
    return EAGAIN;
```
**must be kept**. Without it, the background callout issues a CMD53
read to an empty F2 FIFO every 50 ms. Those reads do not return a
clean all-zeros buffer — they return garbage that can partially pass
the `len`/`len_inv` check, corrupting the SDIO CAM queue state.
This was confirmed empirically: removing the gate (commit `b707160`)
caused a reproducible `camq_remove: out-of-bounds index -3` kernel
panic on both cold boot and reload. The gate was restored in commit
`d137875`.

**The gate is safe** as long as `cyw_sdpcm_attach()` — which starts the
callout — is only called after the FWREADY handshake. Because
FWREADY implies the gate bits are set (see above), the callout will
never see a false EAGAIN from the gate under normal attach ordering.

Linux does not need this gate because:

1. Its interrupt is **edge-triggered** off the SDIO IRQ line, not
   level-polled from INTSTATUS. The chip raises the IRQ when a
   response is ready; INTSTATUS is read only in the ISR/DPC, by which
   time the frame-available bits are set.
2. `brcmf_sdio_readframes()` also uses the SDPCM header `len`/`len_inv`
   pair as a secondary empty indicator after any IRQ-driven read, so
   a spurious read does not corrupt anything in the Linux path.

### Doorbell direction

Direction is from the chip's perspective:

- **TOSBMAILBOX** (`SMB_*`): host writes to wake/notify the chip.
  We write `SMB_HOST_INT (0x2)` after each F2 frame to nudge the
  firmware. Linux additionally writes `SMB_NAK / SMB_INT_ACK / …`
  bits during flow-control negotiation.
- **TOHOSTMAILBOXDATA**: chip writes to inform the host. The
  `HMB_DATA_*` bits encode firmware-up, NAK-handled, FWHALT, etc.

The fw-handshake value our driver prints is from `TOHOSTMAILBOXDATA`.
Relevant bits in the low nibble (T:):

| Constant             | Value  | Meaning |
|----------------------|--------|---------|
| `HMB_DATA_DEVREADY`  | `0x02` | SDPCM dispatcher started — set by the firmware immediately after ARM release, including on reload from TCM state. **This value is retained in the register across `cyw_arm_halt` and is still present when the next `kldload` starts downloading firmware.** |
| `HMB_DATA_FWREADY`   | `0x08` | WL init complete — firmware ready to accept IOVARs. Only written by the newly-downloaded firmware ~45 ms after ARM release. **This is the correct signal to wait for.** |

The upper nibble (`0x00040000`) is chip-revision-coded and opaque; we
do not mask or test it.

**Timing (T:):**

| Event                        | Cold boot | Reload (after `cyw_arm_halt`) |
|------------------------------|-----------|-------------------------------|
| `TOHOSTMAILBOXDATA` on entry (pre-download, ARM halted) | `0` | `0x00040002` (stale `DEVREADY` from previous firmware run) |
| After ARM release, t=0 ms   | `0x00040002` | `0x00040002` (still stale) |
| After ARM release, t=~45 ms | `0x00040008` (`FWREADY`) | `0x00040008` (`FWREADY`) |
| After ARM release, t=~65 ms | `0x00040008` (cold boot is slightly slower) | — |

The correct handshake waits for `TOHOSTMAILBOXDATA & HMB_DATA_FWREADY`
(commit `1b2f833`). The previous condition (`tohost != 0`) fired
immediately on the stale `DEVREADY` value during reload, causing the
`ver` IOVAR to be sent before the firmware was ready and therefore
always timing out (`firmware: unknown`).

---

## 8. ARM CR4 + D11 reset state machine (L chip.c:1052- and 1307-)

The host has fine-grained reset control over both cores via the BCMA
**RESET_CTL** and **IOCTL** registers, each at fixed BCMA offsets
relative to the per-core register window.

```
   ┌── disable_arm(CR4) ──┐         ┌── enable_arm(CR4) ──┐
   │ 1. ioctl |= CPUHALT  │         │ 1. ioctl = FGC|CLK   │
   │ 2. reset_ctl = RESET │   ──►   │ 2. reset_ctl = 0     │
   │ 3. (keep CPUHALT)    │         │ 3. ioctl = CLK       │
   └──────────────────────┘         │    (release halt)    │
                                    └──────────────────────┘
```

`set_passive` = `disable_arm(CR4)` + `coredisable(D11, PHYRESET|PHYCLOCKEN, PHYCLOCKEN)`.
The D11 sequence holds PHYRESET while clocked, then clears RESET_CTL,
then leaves only PHYCLOCKEN — D11 ends in a powered, clocked, idle
state.

This is **exactly** what `cyw_arm_halt` in our `cyw43455_sdio.c:233`
does, and is the equivalent of Linux's `brcmf_sdio_remove` →
`brcmf_chip_set_passive` → `brcmf_chip_cr4_set_passive`.

### What `set_passive` does NOT do (T:)

`set_passive` halts the running firmware but does **not**:

- Toggle WL_REG_ON / power-cycle the chip. Pi-class Linux installations
  rely on the platform's WL_REG_ON GPIO (defined in DT) to actually
  power-cycle the radio between module loads. **We have no WL_REG_ON
  control surface in our driver** — the Pi5 SDIO bridge does not
  expose a power-enable GPIO accessible from the sdiob bus.
- Reset PMU ChipControl 3 (where SR-capability lives). PMU CC3 is
  persistent across `set_passive`.
- Clear stale F2 FIFO contents.

This explains the observed cold-vs-reload differences (T:):

| Symptom                  | Cold boot              | After `cyw_arm_halt` reload |
|--------------------------|------------------------|------------------------------|
| `PMU CC3`                | `0xe1ab1f7c` (bit 2 set) | `0xe1b1f7c9` (bit 2 clear) |
| `SR capable`             | yes                    | no |
| `pre-download TOHOSTMAILBOXDATA` (ARM halted) | `0` | `0x00040002` (stale `DEVREADY`) |
| `fw handshake` time (waiting for `FWREADY`) | ~65 ms | ~45 ms |
| `INTSTATUS` at `FWREADY` | `0x20c000c0` (gate bits set) | `0xa0400080` or `0x20c000c0` (gate bits set) |

The PMU CC3 bit-2 (`PMU_CC3_SR_ENGINE_ENABLE`, chip.c:1430) is
**cleared by the running firmware during normal operation** — Linux
reads it before firmware download to decide whether to allocate the
SR scratch banks. After our halt+reload, the bit is still cleared
from the previous firmware's run; the chip is not power-cycled, so
PMU state persists.

Whether to "re-enable" SR by writing PMU CC3 from the host before
the next firmware download is **not done in Linux** (Linux relies on
WL_REG_ON for full reset). The remaining SR issue is **not blocking**
— the firmware runs correctly without SR on reload; `kldunload` /
`kldload` cycles work cleanly (5-cycle test verified, T:).

**What actually fixed reliable reload (T:):**

1. **Wait for `HMB_DATA_FWREADY` (bit 3)** in the post-ARM-release
   handshake (commit `1b2f833`).  `TOHOSTMAILBOXDATA` retains the
   previous firmware's `HMB_DATA_DEVREADY` (0x02) value across
   `cyw_arm_halt` — without this fix the handshake exited immediately
   on the stale value and the first IOVAR timed out.
2. **Keep the INTSTATUS gate** in `cyw_sdpcm_recv_one` (restored in
   commit `d137875`).  The gate is safe because INTSTATUS always has
   the data-available bits set by the time `FWREADY` is written
   (~45 ms after ARM release on reload, ~65 ms on cold boot).
   Removing the gate (as commit `b707160` briefly did) causes the
   50 ms callout to issue CMD53 reads to an empty F2 FIFO, corrupting
   the SDIO CAM queue and panicking the kernel
   (`camq_remove: out-of-bounds index -3`).

The SR-capability loss on reload remains an open cosmetic issue (see
§11 item 2) but does not affect driver correctness.

---

## 9. Per-IOVAR/IOCTL lifecycle (BCDC over SDPCM CTRL)

```
   host wants "ver" GET, txid=N:
   ┌────────────────────────────────────────────────────────────────┐
   │ wait for credit (rx_max − tx_seq > 0)                          │
   │ build [SDPCM hdr | BCDC hdr | "ver\0" | zero-pad]              │
   │ CMD53 F2 write → frame in firmware's RX FIFO                   │
   │ CMD52 backplane TOSBMAILBOX ← SMB_HOST_INT  (doorbell)         │
   └──────────────────────┬─────────────────────────────────────────┘
                          │
                  firmware processes, queues response
                          │
                          ▼
   ┌────────────────────────────────────────────────────────────────┐
   │ firmware: sets INTSTATUS bits (cold-boot only — see §7 quirk)  │
   │ firmware: writes response frame to F2 host-readable FIFO       │
   │ firmware: asserts SDIO IRQ line                                │
   └──────────────────────┬─────────────────────────────────────────┘
                          │
                  Linux: ISR → DPC → readframes
                  Ours:  50 ms callout → taskqueue thread → recv_one
                          │
                          ▼
   ┌────────────────────────────────────────────────────────────────┐
   │ CMD53 F2 read CYW_F2_BLKSIZE (=64) bytes                       │
   │ validate SDPCM hdr (len, ~len_inv, len ≥ HDR_LEN)              │
   │ if frame > BLKSIZE: CMD53 again for the tail                   │
   │ update rx_max from hdr->credit                                 │
   │ dispatch by hdr->chan_flags & 0xF:                             │
   │   CTRL: match BCDC flags.txid against ioctl_wait_id; cv_signal │
   │   EVENT: queue for net80211 event handler                      │
   │   DATA: ieee80211_input (in a future milestone)                │
   └────────────────────────────────────────────────────────────────┘
```

### Boot-time vs runtime path (our driver)

| Path        | `sc->sdpcm_running` | Caller     | RX side          |
|-------------|---------------------|------------|------------------|
| Boot-time   | `false`             | `cyw_fil_txrx` polls `cyw_sdpcm_recv_one` for up to 300 × 10 ms | Same thread |
| Runtime     | `true`              | `cyw_fil_txrx` writes frame, `cv_timedwait(3 s)` | Dedicated per-device taskqueue (commit `0ffb512`) |

**T:** The runtime path **must** run on a thread other than
`taskqueue_thread`. `sdiob` enqueues device discovery on
`taskqueue_thread` (sdiob.c:1050), and our `cv_timedwait` runs in
that thread; if the RX task is also on `taskqueue_thread`, the wait
self-starves and the IOCTL always times out. Fixed by allocating a
private `taskqueue_create("cyw43455_rx", …, taskqueue_thread_enqueue,
&sc->rx_tq)` with one dedicated thread.

---

## 10. Detach — what we have to do, ordered

Mirrors `brcmf_sdio_remove` (L sdio.c:4765). Confirmed working in
our `cyw_sdio_detach` (commit `708f92a`):

1. `cyw_cfg_detach` — `ieee80211_ifdetach` while net80211 still has
   the SDPCM task to back its `ic_transmit` (stub).
2. `cyw_sdpcm_detach` — clear `sdpcm_running`, `callout_drain`,
   `taskqueue_drain` + `taskqueue_free` the private RX queue. Wake
   any sleeping fwil caller with ENXIO so they don't hang on the way
   out.
3. `cyw_sdio_enable_clock(sc)` + `DELAY(20000)` — ensure ALP for the
   passive-reset writes and give the chip a Linux-equivalent settle
   window.
4. `cyw_arm_halt(sc)` — `set_passive`: halt CR4, reset D11.
5. `CHIPCLKCSR=0` — release the host's HT/ALP request.
6. `sdio_disable_func(F2)`; `sdio_disable_func(F1)`.

Steps **3 → 4 → 5 → 6 ordering is required**. If the SDIO functions
are disabled before the ARM is halted, the running firmware will (a)
clear KSO and (b) modify PMU CC3 such that the next attach finds the
chip in the "reload" state described in §8.

---

## 11. Open issues and unknowns

These are explicitly **not** yet confirmed against documentation or
tested:

1. **F2 FIFO flush on detach** — we don't currently drain F2 before
   `disable_func(F2)`. If responses to in-flight IOVARs are pending,
   they may be left in the chip's FIFO and reappear on the next
   attach. (**Not** a contributor to the CAM panic seen under repeated
   reload — that panic was caused by the background callout issuing
   CMD53 reads to an empty F2 FIFO after the INTSTATUS gate was
   wrongly removed; see T: note in §7.  Whether stale FIFO contents
   cause any observable problem in practice remains untested.)
2. **PMU CC3 restore for SR** — speculation that writing back the
   cold-boot CC3 value before firmware download would restore SR
   capability on reload. Not attempted; no documentation found.
3. **TOSBMAILBOX bit semantics** — Linux uses `SMB_INT_ACK`, `SMB_NAK`,
   `SMB_USE_OOB`, `SMB_DEV_INT` and `SMB_HOST_INT`. Our code only
   writes `SMB_HOST_INT`. Whether the firmware actually needs any of
   the other bits for our use case has not been tested.
4. **Why `INTSTATUS` bit 22 is set in both cold-boot and reload**
   (`0x00400000`). This bit is not named in Linux's headers; it
   appears to be reserved. We do not act on it.
5. **TOHOSTMAILBOXDATA layout above the low nibble** — the values
   `0x00040008` vs `0x00040002` differ in bits 0-3 only. The 0x40000
   constant component is unexplained.
6. **~~`fw handshake at 0 ms` on reload~~** — **RESOLVED** (commit
   `1b2f833`).  Root cause: `TOHOSTMAILBOXDATA` retains the previous
   firmware's `HMB_DATA_DEVREADY` value (0x02, bit 1) across
   `cyw_arm_halt`.  The old handshake polled for `tohost != 0` so it
   exited immediately on the stale value, before the newly-downloaded
   firmware had initialised.  Fix: poll for `HMB_DATA_FWREADY`
   (bit 3, 0x08) specifically — this bit is only written by newly-
   downloaded firmware ~45 ms after ARM release.  The symptom
   (`fw handshake at 0 ms` → first IOVAR timeout → `firmware:
   unknown`) is no longer reproducible; every reload now shows
   `fw handshake at ~45 ms` and correct firmware version.
7. **~~`escan` returns BCME_NOTUP~~** — **RESOLVED** (see §14.6).
   Root cause: the firmware will not bring the BSS into a scan-ready
   state without the **CLM regulatory blob** uploaded at attach time.
   Fix: `cyw_clm_load()` (mirrors Linux `brcmf_c_process_clm_blob`),
   called after dongle-init IOVARs and before net80211 attach.  The
   blob is shipped as
   `/boot/firmware/cyw43455/brcmfmac43455-sdio.clm_blob` and uploaded
   via chunked `clmload` IOVAR (256-byte chunks to force byte-mode
   CMD53 in sdiob — block-mode CMD53 fails in the boot-time polling
   path).  After CLM upload, `escan` succeeds and `ifconfig wlan0
   scan` returns APs with full HTCAP/VHTCAP/RSN capability
   information.

---

## 12. Firmware delivery — firmware(9) + `/boot/firmware` (T:)

The three blobs (`brcmfmac43455-sdio.bin` ~609 KB,
`brcmfmac43455-sdio.txt` NVRAM ~1.7 KB, `brcmfmac43455-sdio.clm_blob`
~2.6 KB) live in `/boot/firmware/cyw43455/` and are obtained through the
FreeBSD **firmware(9)** subsystem — `cyw_fw_get()` / `firmware_get()` in
`cyw43455_fw.c` — **not** by the driver opening files itself.

### Why not direct VFS

The driver originally read the blobs with `vn_open()`/`vn_rdwr()` at
attach time. That works for a post-boot `kldload` but **panics when the
module is preloaded from `/boot/loader.conf`**: SDIO child attach runs
in the `sdiobdiscover` taskqueue (`dev/sdio/sdiob.c`), a kernel thread
with no `fd_cdir`/`fd_rdir`, so `namei()`/`cache_fplookup()` faults at
`far=0x4`. The fault is fatal because `devclass_add_driver` runs under
`bus_topo_lock()` — which is `Giant` — so the abort handler reports
`panic: data abort in critical section or under mutex`.

`firmware(9)` avoids this: a **preloaded** image is returned straight
from RAM (no VFS, no sleep, safe under Giant), and a lazy disk read is
performed on the dedicated `firmware_tq` thread (which is given
filesystem context via `pwd_ensure_dirs()` on the `mountroot` event) —
never on the attach thread. `subr_firmware.c`'s `fw_path` is literally
`/boot/firmware/`, the exact layout this board uses.

### Two delivery paths (both verified)

- **`kldload` after boot** — `firmware_get("cyw43455/brcmfmac43455-sdio.bin")`
  finds nothing registered, defers to `firmware_tq`, which reads
  `/boot/firmware/cyw43455/...` (`fw_path` + the subdir-relative name).
- **Preloaded at boot** — the loader preloads each blob as
  `-t firmware`; `firmware_binary_files()` registers them at
  `SI_SUB_DRIVERS/SI_ORDER_FIRST` under their **absolute** path, and
  `subr_firmware.c lookup()` trailing-path-matches that against the
  driver's subdir-relative request. The data is in RAM before any
  driver attaches, so the early preloaded attach succeeds with no VFS.

Requesting names **with the `cyw43455/` subdir** is deliberate — it
makes both the preload trailing-match and the lazy `fw_path + name` read
resolve to the same file. `cyw_fw_get()` passes `FIRMWARE_GET_NOWARN` to
suppress `subr_firmware`'s misleading "could not load firmware image,
error 8" (the `.ko`-module probe that always fails since we ship no
firmware `.ko`); genuine absence is reported by our own NULL-return log.

`MODULE_DEPEND(cyw43455, firmware, 1, 1, 1)` guarantees the subsystem is
present (it is compiled into the RPI5 kernel).

### Enabling WiFi at boot — `/boot/loader.conf`

```
# Preload the three blobs as firmware(9) images, then the driver.
brcm_fw_bin_load="YES"
brcm_fw_bin_name="/boot/firmware/cyw43455/brcmfmac43455-sdio.bin"
brcm_fw_bin_type="firmware"
brcm_fw_nvram_load="YES"
brcm_fw_nvram_name="/boot/firmware/cyw43455/brcmfmac43455-sdio.txt"
brcm_fw_nvram_type="firmware"
brcm_fw_clm_load="YES"
brcm_fw_clm_name="/boot/firmware/cyw43455/brcmfmac43455-sdio.clm_blob"
brcm_fw_clm_type="firmware"
cyw43455_load="YES"
```

Confirm after boot: `kenv | grep -E 'cyw43455_load|brcm_fw'`,
`kldstat | grep cyw43455` (low Id ⇒ loader-preloaded, not devmatch),
and `dmesg | grep -c 'error 8'` should be **0** (firmware came from the
preload registry, not the lazy fallback).

### Swapping the regulatory blob

The blobs stay on the filesystem, so the per-deployment `.clm_blob` (and
NVRAM) can be replaced without rebuilding the driver. A `kldload`-loaded
driver picks up the new file immediately; for the preloaded boot path,
one reboot re-preloads the updated file. A missing `.clm_blob` is
non-fatal (limited channels) — see §8.6.

---

## 13. Linux firmware command sequence from boot to first scan

Source files: `brcmfmac/common.c` (`brcmf_c_preinit_dcmds`),
`brcmfmac/core.c` (`brcmf_bus_started`, `brcmf_netdev_open`),
`brcmfmac/cfg80211.c` (`brcmf_config_dongle`, `brcmf_cfg80211_scan`,
`brcmf_do_escan`, `brcmf_cfg80211_change_iface`).

### Phase 1 — SDIO bus attach (no firmware commands yet)

Linux's `brcmf_sdio_probe` downloads firmware, releases ARM, waits for
`HMB_DATA_FWREADY`, enables F2, then calls `brcmf_bus_started`.

| Step | Action | Firmware state after |
|------|--------|----------------------|
| 1 | ARM release (IOCTL cleared, RESET_CTL=0) | Firmware booting, BSS not yet configured |
| 2 | Wait for `HMB_DATA_FWREADY` (~45–65 ms) | Firmware running, SDPCM/BCDC stack ready |
| 3 | F2 enabled, `HOSTINTMASK` written | F2 RX path live; firmware queues E_IF ADD |
| 4 | `brcmf_bus_change_state(BRCMF_BUS_UP)` | Host flag only — no firmware command |

### Phase 2 — `brcmf_c_preinit_dcmds` (common.c:268)

Called from `brcmf_bus_started` after F2 is ready. **No WLC_DOWN, no
WLC_UP, no WLC_SET_INFRA issued here.** The BSS is left in whatever
initial state the firmware chose after boot.

| Step | Command / IOVAR | Value | Notes |
|------|-----------------|-------|-------|
| 1 | `cur_etheraddr` GET | — | Read factory MAC |
| 2 | `C_GET_REVINFO` (cmd 318) | — | Read chip/firmware revision |
| 3 | CLM blob download | — | `brcmf_c_process_clm_blob` via chunked `clmload` IOVAR + `clmload_status` verify.  **The Pi 5 firmware package DOES ship a CLM blob** (`cyfmac43455-sdio.clm_blob`, 2676 B) and it is **required** — without it, `escan` returns BCME_NOTUP unconditionally (see §14.6). |
| 4 | `ver` GET | — | Read firmware version string |
| 5 | `mpc` SET | **1** (enabled) | MPC on by default; disabled only right before escan |
| 6 | `event_msgs` GET + SET | add `E_IF` bit | Subscribe to firmware events |

**Observation:** Linux sets `mpc=1` at init, and firmware starts in its
own DEFAULT state.  Linux does **not** issue `WLC_DOWN`, `WLC_SET_INFRA`,
`btc_mode`, `allmulti`, `roam_off`, or any form of `pm` here.

### Phase 3 — `brcmf_cfg80211_attach` / `brcmf_net_attach`

Pure host-side setup (cfg80211/wiphy registration, net_device ops, no
firmware commands). `brcmf_netdev_open` is registered as `ndo_open`.

### Phase 4 — First `ifconfig wlan0 up` → `brcmf_netdev_open`

`brcmf_netdev_open` → `brcmf_cfg80211_up` → `__brcmf_cfg80211_up`
(cfg80211.c:8019):

```
set_bit(BRCMF_VIF_STATUS_READY, &ifp->vif->sme_state)  ← host-only flag
brcmf_config_dongle(cfg)                                ← firmware sequence below
```

`brcmf_config_dongle` (cfg80211.c:7968), in order:

| Step | Command / IOVAR | Value | Source | Notes |
|------|-----------------|-------|--------|-------|
| 1 | `BRCMF_C_UP` (cmd 2) | 0 | cfg80211.c:7985 | Bring BSS up. Arg is 0 (ignored by firmware). |
| 2 | `BRCMF_C_SET_SCAN_CHANNEL_TIME` (cmd 185) | 40 ms | `brcmf_dongle_scantime` | Active dwell per channel |
| 3 | `BRCMF_C_SET_SCAN_UNASSOC_TIME` (cmd 187) | 40 ms | `brcmf_dongle_scantime` | Unassoc dwell |
| 4 | `BRCMF_C_SET_SCAN_PASSIVE_TIME` (cmd 258) | 120 ms | `brcmf_dongle_scantime` | Passive dwell |
| 5 | `BRCMF_C_SET_PM` (cmd 86) | 0 (PM_OFF) or 2 (PM_FAST) | cfg80211.c:7989 | Depends on `cfg->pwr_save`; Pi 5 default: PM_OFF=0 |
| 6 | `bcn_timeout` IOVAR | 4 (roam off) or 8 (roam on) | `brcmf_dongle_roam` | Beacon loss timeout |
| 7 | `roam_off` IOVAR | 1 (disabled) | `brcmf_dongle_roam` | Suppress internal roam |
| 8 | `BRCMF_C_SET_ROAM_TRIGGER` (cmd 55) | `[-75 dBm, BRCM_BAND_ALL]` | `brcmf_dongle_roam` | Two `__le32` words |
| 9 | `BRCMF_C_SET_ROAM_DELTA` (cmd 57) | `[20, BRCM_BAND_ALL]` | `brcmf_dongle_roam` | Two `__le32` words |
| 10 | `BRCMF_C_SET_INFRA` (cmd 20) | 1 (infra/STA) | `brcmf_cfg80211_change_iface` | Called only for non-AP modes; not called if `iftype==AP` |
| 11 | arp/nd offload IOVARs | — | `brcmf_configure_arp_nd_offload` | `arpoe`, `arp_ol`, `ndoe` — offload to firmware |
| 12 | `BRCMF_C_SET_FAKEFRAG` (cmd 219) | 1 | cfg80211.c:8007 | Frameburst / fake fragmentation |
| — | `cfg->dongle_up = true` | — | — | Guards single-run; subsequent ifconfig up/down cycles skip this block |

**Critical note on WLC_DOWN:** Linux **never issues WLC_DOWN** in the
preinit or config_dongle paths for a STA interface.  `BRCMF_C_DOWN`
only appears in the AP-mode setup code (`brcmf_cfg80211_start_ap`,
cfg80211.c:5218/5394) and the P2P path (p2p.c:434). Issuing WLC_DOWN
at attach time is not part of the Linux pattern for STA operation.

**Critical note on WLC_SET_INFRA ordering:** `BRCMF_C_SET_INFRA` is
issued **after** `BRCMF_C_UP`, inside `brcmf_cfg80211_change_iface`.
Linux never issues SET_INFRA before C_UP; the order is
C_UP → scan timing → SET_PM → roam → SET_INFRA → fakefrag.

### Phase 5 — `ifconfig wlan0 scan` → `brcmf_cfg80211_scan` (cfg80211.c:1516)

| Step | Action | Notes |
|------|--------|-------|
| 1 | `check_vif_up(vif)` | Returns false (→ -EIO) if `VIF_STATUS_READY` not set; this flag was set in Phase 4 step 1 |
| 2 | Check `SCAN_STATUS_BUSY/ABORT/SUPPRESS` | Abort if scan already in progress |
| 3 | `brcmf_do_escan(vif->ifp, request)` | Calls `brcmf_scan_config_mpc(ifp, 0)` **first** |
| 4 | `brcmf_scan_config_mpc(ifp, 0)` | Sets `mpc=0` (radio stays awake) — only if `NEED_MPC` quirk is set for this chip |
| 5 | `escan` IOVAR SET | Firmware-driven scan; results delivered as `E_ESCAN_RESULT` events |
| 6 | On completion or error: `brcmf_scan_config_mpc(ifp, 1)` | Restore mpc=1 |

**`NEED_MPC` quirk:** Checked via `brcmf_feat_is_quirk_enabled(ifp,
BRCMF_FEAT_QUIRK_NEED_MPC)`.  This is set for chips that need MPC
toggled around scans.  If the quirk is not set, `mpc` is never touched
by the scan path.  For the CYW43455 (43454 silicon), check whether this
quirk applies.

### Comparison: Linux vs. cyw43455 driver (current state, post-experiments)

After §14 experiments the cyw43455 driver has been brought to near-parity
with Linux for STA-mode bring-up:

| Phase | Linux | cyw43455 (current) | Delta |
|-------|-------|--------------------|-------|
| Attach: WLC_DOWN | ✗ never | ✗ removed | match |
| Attach: WLC_SET_INFRA | ✗ never | ✗ removed | match |
| Attach: mpc | mpc=1 | (left at firmware default = 1) | match |
| Attach: btc_mode | ✗ never | btc_mode=0 | Extra; non-fatal |
| Attach: roam_off | ✗ never | roam_off=1 | Extra (Linux does it in config_dongle) |
| Attach: pm | ✗ never | pm=0 (returns BCME_UNSUPPORTED on 7.45.265) | Extra; no-op |
| Attach: allmulti | ✓ (in netdev context) | ✓ | Same intent |
| Attach: WLC_UP | ✗ never | ✗ removed | match |
| ifconfig up: WLC_UP | ✓ config_dongle step 1 | ✓ in `cyw_parent` | match |
| ifconfig up: SET_INFRA | ✓ via change_iface, after C_UP | ✓ in `cyw_parent` after WLC_UP | match |
| ifconfig up: scan timing, roam, fakefrag, arp_nd | ✓ config_dongle | ✗ (`#if 0`) | Believed non-critical for scan; reinstate as Milestone 2.5 needs |
| Scan: mpc=0 before escan | ✓ only when `NEED_MPC` quirk set (chip 0x4329 only) | ✗ | match — quirk does not apply to 0x4345/43454 |
| Scan: escan IOVAR payload | V2 if `scan_ver` IOVAR exists, else V1 | V1 byte-for-byte (see §14.4) | matches V1; V2 capability not probed |

## 14. BCME_NOTUP investigation (T:)

`ifconfig wlan0 scan` causes the firmware to reject the `escan` IOVAR
with `BCME_NOTUP` (`status=0xfffffffc`, -4 = "BSS not up").  The chain
of experiments below has reached the point where every host-side
sequencing variable we can change matches Linux exactly, the firmware
ack's both interface state changes via E_IF events, and BCME_NOTUP
still appears.  Recorded here so future work does not re-derive the
same ruled-out hypotheses.

### 14.1 What is observed (current driver state, both cold-boot and reload)

```
firmware: 7.45.265 (28bca26 CY) FWID 01-b677b91b   (attach)
MAC: b8:27:eb:74:f2:6c                             (attach)
cyw_parent: WLC_UP ok                              (ifconfig up)
cyw_parent: pausing 200ms
  → E_IF action=ADD(1), bsscfgidx=0, role=STA      (firmware ack)
cyw_parent: WLC_SET_INFRA ok
  → E_IF action=CHANGE(3), bsscfgidx=0             (firmware ack)
cyw_parent: dongle_up set
cyw_scan_start: dongle_up=1
escan IOVAR → status=0xfffffffc (BCME_NOTUP)
```

The sequence is the same on cold-boot (PMU SR-capable) and on
reload-after-`kldunload` (PMU SR not re-inited).  SR state is
irrelevant to BCME_NOTUP.

### 14.2 Hypotheses tested and ruled out

| # | Hypothesis | Test | Outcome |
|---|------------|------|---------|
| 1 | Race: scan_tq fires escan before `cyw_parent` finishes WLC_UP | Added trace logs through `cyw_parent` and `cyw_scan_start` | No race. `cyw_scan_start: dongle_up=1` always logs after `cyw_parent: dongle_up set`. |
| 2 | WLC_DOWN at attach (not in Linux) wedges the BSS | Removed WLC_DOWN from `cyw_attach` | No change in BCME_NOTUP |
| 3 | WLC_SET_INFRA at attach (not in Linux) wedges the BSS | Removed WLC_SET_INFRA from `cyw_attach` | No change |
| 4 | WLC_UP at attach (not in Linux) is the wrong timing | Moved WLC_UP from `cyw_attach` to `cyw_parent` exclusively | No change |
| 5 | mpc=0 at attach interferes with BSS UP state machine | Removed mpc=0 — leaves firmware default (mpc=1); NEED_MPC quirk only applies to chip 0x4329 in Linux, not 0x4345/43454 | No change |
| 6 | UP/SET_INFRA ordering wrong | Re-enabled SET_INFRA in `cyw_parent` after WLC_UP (matches Linux `brcmf_config_dongle`) | Now sees E_IF CHANGE in addition to E_IF ADD, but escan still BCME_NOTUP |
| 7 | full config_dongle sequence required (scan timing, PM, roam, fakefrag) | Issued full sequence in `cyw_parent` | Some commands returned BCME_BADARG (ROAM_TRIGGER, ROAM_DELTA), no change in BCME_NOTUP. Sequence stripped to UP+SET_INFRA. |
| 8 | E_IF event needs host follow-up to firmware | Read Linux `brcmf_fweh_handle_if_event`, `brcmf_proto_bcdc_add_if`, `brcmf_fws_add_interface`, `brcmf_notify_vif_event`: **all host-side bookkeeping only** (MAC descriptor init, packet queue init, cfg80211 pointer wiring, wake_up on vif_wq).  No firmware commands issued in response to E_IF ADD or CHANGE. | Linux does nothing firmware-facing; our silence in response is correct. |
| 9 | escan IOVAR payload malformed | Compared `cyw_escan_params_le` field-by-field against Linux V1 `brcmf_scan_params_le` / `brcmf_escan_params_le`. See §14.4. | Payload byte-for-byte correct for V1 layout. |
| 10 | Reload-vs-cold-boot SR state difference matters | Tested both via true power cycle | Identical result |

### 14.3 What is consistent with the firmware's behavior

- WLC_UP succeeds (status=0).
- Firmware sends `E_IF action=ADD, bsscfgidx=0, role=STA` after WLC_UP.
- WLC_SET_INFRA(1) succeeds.
- Firmware sends `E_IF action=CHANGE, bsscfgidx=0` after WLC_SET_INFRA.
- escan returns BCME_NOTUP unconditionally on every run.

The firmware is acknowledging interface ADD and mode CHANGE at the
SDPCM event layer, but its escan precondition (whatever it is) treats
the BSS as not UP.

### 14.4 escan payload verification (Linux V1 vs. ours)

Linux's `brcmf_run_escan` (cfg80211.c:1428) builds a V2 payload by
default, then **downgrades to V1** if `BRCMF_FEAT_SCAN_V2` is *not*
detected.  V2 detection is by probing the `"scan_ver"` IOVAR via
`brcmf_feat_iovar_int_get` (feature.c:350).  Our driver does not
probe `scan_ver`, so we have always sent V1 — which is what most
brcmfmac firmwares actually expect.

V1 vs V2 fixed-size struct layout:

| Field | V1 `brcmf_scan_params_le` | V2 `brcmf_scan_params_v2_le` |
|-------|--------------------------|------------------------------|
| (header) | — | `__le16 version` + `__le16 length` |
| `ssid_le` | offset 0 | offset 4 |
| `bssid[6]` | offset 36 | offset 40 |
| `bss_type` | `s8` at 42 | `s8` at 46 |
| `scan_type` | `u8` | `u8 pad` + `__le32` |
| `nprobes`/times | `__le32`×4 | `__le32`×4 |
| `channel_num` | `__le32` | `__le32` |
| Fixed size | **64 B** | **72 B** |

Our `cyw_scan_params_le` matches V1.  Full escan payload (V1, no
channel/ssid lists) = 4 + 2 + 2 + 64 = **72 bytes**.

Field-by-field compare against Linux `brcmf_escan_prep` + V1:

| Field | Our value | Linux V1 | Match |
|-------|-----------|----------|-------|
| escan `version` | `htole32(1)` | `cpu_to_le32(BRCMF_ESCAN_REQ_VERSION=1)` | ✓ |
| escan `action` | `htole16(1)` START | `cpu_to_le16(WL_ESCAN_ACTION_START=1)` | ✓ |
| escan `sync_id` | incrementing `htole16` | fixed `cpu_to_le16(0x1234)` | ✓ (value arbitrary) |
| `ssid_le.SSID_len` | 0 | 0 | ✓ |
| `bssid` | 0xff×6 | `eth_broadcast_addr` 0xff×6 | ✓ |
| `bss_type` | 2 (DOT11_BSSTYPE_ANY) | 2 (`DOT11_BSSTYPE_ANY`) | ✓ |
| `scan_type` | 0 (active) | 0 (`BRCMF_SCANTYPE_ACTIVE`) | ✓ |
| `nprobes` | `htole32(-1)` | `cpu_to_le32(-1)` | ✓ |
| `active_time` / `passive_time` / `home_time` | `htole32(-1)` each | `cpu_to_le32(-1)` each | ✓ |
| `channel_num` | 0 (all channels) | 0 (`!n_channels && !n_ssids`) | ✓ |
| payload size | 72 B | 64+8 = 72 B for V1 | ✓ |

BCME_NOTUP is **not** a malformed-request error (that would be
BCME_BADARG=-13).  Payload verification closes this hypothesis.

### 14.5 Hypotheses remaining (not yet tested)

These are speculative and recorded to seed future work:

1. **`scan_ver` probe** — verify which scan-params version this
   firmware actually expects.  Send `"scan_ver"` IOVAR GET via
   `cyw_fil_iovar_int_get`; if it returns 0 the firmware supports
   V2 and we are sending the wrong layout (though the resulting
   firmware error would more plausibly be BCME_BADARG, not
   BCME_NOTUP).
2. **CLM blob** — Linux's `brcmf_c_process_clm_blob`
   (common.c:`brcmf_c_preinit_dcmds` step 3) loads `clmver` data into
   the firmware.  Pi 5 firmware package may or may not ship a CLM
   blob.  Missing CLM may cause the firmware to refuse PHY/scan
   operations without raising a BCME_BADARG — it could surface as
   BCME_NOTUP instead.  We do not perform this step.
3. **NVRAM `regulatory` field** — the NVRAM `cyw43455-rpi.txt` we ship
   may not declare a regulatory domain the firmware accepts.  No
   regulatory domain → firmware refuses to enable any scan channels
   → BSS effectively unscan-able.  Untested.
4. **`country` IOVAR** — Linux sets country (cfg80211 reg_notifier)
   before scanning is allowed.  We do not.  Firmware may treat
   "no country code committed" as "BSS not up for scan".  Easy to
   test with `country` IOVAR set to e.g. `"US"`.
5. **`bsscfg:` prefixed IOVARs** — some IOVARs require a bsscfg-index
   prefix in the IOVAR name (the IOVAR name buffer carries
   `"bsscfg:foo\0\0<index>"`).  Our escan path sends the IOVAR
   unprefixed.  Linux does the same for escan, so this is unlikely.
6. **`event_msgs_ext`** — Linux probes for this on 43012 chips and
   uses the extended event mask.  We do not.  Probably irrelevant
   on 0x4345.

### 14.6 Resolution: CLM blob is required (T:)

Hypothesis §14.5 item 2 was **confirmed as the root cause**.  The
firmware refuses every `escan` (and presumably every PHY-touching
operation) with BCME_NOTUP until the CLM regulatory blob has been
loaded.  Other ruled-out hypotheses from §14.2 were red herrings:
the host-side sequencing (UP→SET_INFRA, dongle_up flag, event
ordering) was already correct; only the missing CLM gated the BSS.

**Implementation notes:**

1. **Wire format** — `clmload` IOVAR carries a `dload_data_le` header
   followed by the chunk payload:
   ```
   __le16 flag        (DL_BEGIN=0x0002 on first chunk, DL_END=0x0004 on last,
                       HANDLER_VER=1 in bits[15:12])
   __le16 dload_type  = 2
   __le32 len         (chunk length in bytes)
   __le32 crc         = 0 (firmware doesn't verify)
   uint8_t data[]
   ```
2. **Chunk size = 256 B** — frame overhead is
   `SDPCM(12) + BCDC(16) + "clmload"\0(8) + dload_hdr(12) = 48 B`.
   For a 256-byte chunk the total CMD53 transfer length is
   `ceil(ALIGN4(304)/64)*64 = 320 B < 512`, which keeps `sdiob`
   on **byte-mode CMD53**.  Block-mode CMD53 (any txlen ≥ 512) fails
   in the boot-time polling context where this IOVAR is issued —
   sdiob's F2 `cur_blksize` is 512 regardless of our CCCR write, and
   block-mode reads/writes hang waiting for an interrupt that the
   polling path will never service.
3. **`clmload_status` verify** — after the last chunk, a `clmload_status`
   IOVAR GET must return 0.  Non-zero indicates a corrupt or wrong-chip
   blob.
4. **Firmware file source** — the upstream `cyfmac43455-sdio.clm_blob`
   (Cypress firmware package) renamed to `brcmfmac43455-sdio.clm_blob`
   and installed to `/boot/firmware/cyw43455/`.  At 2676 bytes it is
   small and Pi-5-specific.  Loading is **non-fatal** if the file is
   absent (firmware then falls back to a built-in default regulatory
   table that may permit only a limited channel set).

**Verified working** (cold boot, 2026-05-23):
```
cyw434550: loading CLM blob (2676 bytes)
cyw434550: CLM blob loaded ok
...
ifconfig wlan0 scan → 5 APs across ch 1 + ch 8, HTCAP/VHTCAP/RSN
```

---

## 16. Scanning, chanspec encoding, and WPA2 association — lessons learned

This section records discoveries made during Milestone 2 bring-up
(2026-05-23) that are not obvious from the Linux source or the datasheet.

---

### 16.1 Hidden SSIDs and directed scan

#### Background

The test AP (`YOUR_SSID`) operates as a *hidden* network.  It transmits
beacons with `SSID_len = 0` — the BSSID seen in beacons (call it
`BEACON_BSSID`) is one MAC.  When a STA sends a **directed probe
request** (with the real SSID in the Probe Request body) the AP
responds with a Probe Response from a *different* BSSID
(`PROBE_BSSID`), carrying the real SSID.

A **wildcard escan** (`ssid_le.SSID_len = 0`) only collects beacon
frames.  It therefore finds only `BEACON_BSSID` with `SSID_len = 0`
and never discovers `YOUR_SSID`.  wpa_supplicant's
`bss_get_next_result` rejects anonymous BSSes, so it cannot associate.

#### The `scan_ssid=1` mechanism

wpa_supplicant's `scan_ssid=1` configuration flag causes it to pass the
real SSID to the kernel's scan request as a directed-scan target.
net80211 records this in `ic->ic_scan->ss_nssid` and `ss->ss_ssid[0]`.

Our escan code in `cyw_do_escan()` forwards the directed SSID to the
firmware if net80211 requested one:

```c
if (ss != NULL && ss->ss_nssid > 0 && ss->ss_ssid[0].len > 0) {
    params->params_le.ssid_le.SSID_len =
        htole32((uint32_t)ss->ss_ssid[0].len);
    memcpy(params->params_le.ssid_le.SSID, ss->ss_ssid[0].ssid,
        ss->ss_ssid[0].len);
}
```

With the SSID set in the escan params the firmware sends directed probe
requests; the AP responds with Probe Responses carrying the real SSID and
BSSID.  wpa_supplicant then sees `YOUR_SSID` in the scan results and can
proceed to associate.

#### Hidden-beacon SSID overwrite bug

A secondary bug masked the fix initially: `cyw_parse_ies()` was
unconditionally overwriting `sp->ssid` with the IE-chain SSID pointer,
even when the IE SSID element had `len = 0`.  Every beacon arrival after
a successful Probe Response would therefore erase the cached real
SSID from the BSS entry.

Fix — only override if the IE SSID is non-empty:

```c
case IEEE80211_ELEMID_SSID:
    if (len > 0)          /* never clobber from a hidden beacon */
        sp->ssid = p;
    break;
```

#### Required wpa_supplicant.conf

```
network={
    ssid="YOUR_SSID"
    psk="YOUR_WIFI_PASSWORD"
    key_mgmt=WPA-PSK
    proto=RSN
    pairwise=CCMP
    group=CCMP
    scan_ssid=1          # <-- mandatory for hidden APs
}
```

---

### 16.2 D11AC chanspec encoding

#### Background

The CYW43455 firmware uses **D11AC chanspec** encoding — not the older
D11N encoding used by earlier Broadcom chips.  Our initial implementation
used D11N constants (from `scan.c` comments) and received `EIO` back from
`WLC_SET_SSID`, because the firmware rejected the malformed chanspec.

#### Diagnosis

We added a debug `device_printf` to `cyw_add_bss()` logging the raw
`bi->chanspec` value from each scan result:

```
cyw_add_bss: BSSID=92:3b:ad:1b:87:6f chanspec=0x1008 chan=8 SSID_len=8
```

`0x1008` decoded:
- bits[15:14] = `0b00` → 2.4 GHz band
- bits[13:12] = `0b01` → 20 MHz bandwidth (D11AC BW_20)
- bits[11:8]  = `0b0000` → SB = LLL (primary sub-band)
- bits[7:0]   = `0x08` → IEEE channel 8

This matches the D11AC encoding documented in Linux's `brcmu_d11.c`
(`brcmu_chspec_d11ac_*` constants), not D11N.

#### Correct encoding

For `WLC_SET_SSID` join params, compute the chanspec from the net80211
channel as follows:

```c
/* Build D11AC chanspec for WLC_SET_SSID join params.
 * D11AC encoding: bits[15:14] band, bit[12] BW=20MHz, bits[7:0] IEEE ch.
 * 2.4 GHz: 0x1000 | ch   (band=0b00, BW_20=0x1000)
 * 5 GHz:   0xD000 | ch   (band=0b11, BW_20=0x1000, but upper bit also set)
 *
 * Verified from scan results: ch8/20MHz → 0x1008; ch36/20MHz → 0xD824.
 */
ieee_chan = (chan != NULL) ? chan->ic_ieee : 0;
if (ieee_chan == 0) {
    chanspec = 0;               /* no channel hint; firmware chooses */
} else if (ieee_chan <= 14) {
    chanspec = 0x1000 | (uint16_t)ieee_chan;   /* 2.4 GHz */
} else {
    chanspec = 0xD000 | (uint16_t)ieee_chan;   /* 5 GHz */
}
```

The D11N encoding that was **wrong** and **produced EIO**:

```c
/* WRONG — D11N encoding, rejected by CYW43455 firmware */
chanspec = 0x2B00 | (uint16_t)ieee_chan;   /* 0x2000=2G 0x0800=BW20 0x0300=SB_NONE */
```

#### Key reference

`../raspbian_linux.git/drivers/net/wireless/broadcom/brcm80211/brcmfmac/brcmu_d11.c`
— `brcmu_chspec_d11ac_*` constants and `brcmu_d11n_2_d11ac_chspec()`.

---

### 16.3 WPA2 association — auth path and chanspec requirement

#### What Linux does (`brcmf_cfg80211_connect`)

Linux's `brcmf_cfg80211_connect()` in `cfg80211.c`:

1. Sets `wpaie` IOVAR with the RSN/WPA IE from the association request.
2. Sets `auth` IOVAR to `0` (open system; WPA2 does not use shared-key).
3. Sets `wsec` IOVAR to `4` (AES/CCMP).
4. Sets `wpa_auth` IOVAR to `0x80` (WPA2-PSK).
5. Sets `wsec_pmk` IOVAR with the raw PMK (32 bytes from PBKDF2-SHA1 of
   the passphrase) — the firmware runs the 4-way handshake internally.
6. Issues `WLC_SET_SSID` with a `brcmf_ext_join_params_le` struct that
   includes:
   - `ssid_le` — target SSID
   - `params_le.bssid` — target BSSID (or broadcast)
   - `params_le.chanspec_list[0]` — D11AC chanspec for the target channel
   - `params_le.chanspec_num = 1`

Step 6 is the critical one our driver was missing.

#### Root cause of E_AUTH NO_ACK

Without a chanspec in the join params the firmware did not know which
channel to use for the initial Auth frame.  It chose a wrong or default
channel, transmitted Auth frames there, and received no ACK — hence
`E_AUTH status=NO_ACK`.  The symptom looked like an AP problem but was
entirely in the firmware's channel selection.

Providing the chanspec (`chanspec_num=1, chanspec_list[0]=0x1008` for
ch8/20MHz) caused the firmware to send Auth on the correct channel.

#### Firmware WPA2 supplicant — RETRACTED, see §16.8

The claim originally made here ("firmware handles the 4-way handshake
internally; `sup_wpa` returns BCME_BADARG; must not be set") was
**wrong on both points**.  See §16.8 for the corrected finding.

#### Confirmed success sequence

After adding the D11AC chanspec to `WLC_SET_SSID`:

```
AUTH: SET_SSID chan=8 chanspec=0x1008 returned 0
cyw_newstate: E_AUTH status=0 (OK)
cyw_newstate: E_ASSOC status=0 (OK)
cyw_newstate: E_LINK link=1 (connected)
```

```
ifconfig wlan0
    ssid YOUR_SSID  channel 8 (2447 MHz 20MHz)
    authmode WPA2/802.11i  privacy ON  deftxkey 2
    AES-CCM 2:128-bit  AES-CCM 3:128-bit
```

---

### 16.4 Linux brcmfmac scan/join model — and why `WLC_SET_SSID` alone fails

Symptom we hit (2026-05-24): after a clean wpa_supplicant scan, the
driver's AUTH-state handler issued `WLC_SET_SSID` with a correct
SSID + BSSID + chanspec, and the firmware replied `E_SET_SSID
status=3` (`WLC_E_STATUS_NO_NETWORKS`) on every attempt.  No
`E_AUTH`, no `E_LINK` — the firmware simply could not find the
target BSS.

Cause — established by reading Linux brcmfmac source:

- **Scan path.**  `brcmf_cfg80211_escan_handler`
  (`brcmfmac/cfg80211.c:3626`) collects every partial
  `BRCMF_E_ESCAN_RESULT` into a driver-side staging buffer
  (`cfg->escan_info.escan_buf`).  Only at scan completion does
  `brcmf_inform_bss()` flush it to cfg80211's BSS cache via
  `cfg80211_inform_bss_data()`.  There is **no driver-side BSS
  cache** beyond that.  The firmware has its own internal BSS
  table, but Linux never relies on it being durable across the
  scan→connect boundary.

- **Connect path — `join` IOVAR, not `WLC_SET_SSID`.**
  `brcmf_cfg80211_connect` (`cfg80211.c:2368-2605`) constructs an
  extended struct:
  ```c
  struct brcmf_ext_join_params_le {
      struct brcmf_ssid_le             ssid_le;
      struct brcmf_join_scan_params_le scan_le;   /* nprobes, dwell */
      struct brcmf_assoc_params_le     assoc_le;  /* bssid, chanspec */
  };
  ```
  and submits it via `brcmf_fil_bsscfg_data_set(ifp, "join", …)`
  (`cfg80211.c:2579`).  The embedded `scan_le` block tells the
  firmware to actively probe for the target SSID on the supplied
  chanspec **during the join attempt**, so the firmware no longer
  depends on its internal BSS cache being hot.  This is the
  difference that makes Linux's join robust to seconds-long delays
  between scan completion and the connect call.

- **`WLC_SET_SSID` is Linux's documented fallback.**
  `cfg80211.c:2603` calls `brcmf_fil_cmd_data_set(ifp,
  BRCMF_C_SET_SSID, &join_params, …)` only when the `join` IOVAR is
  unavailable (older firmware lacking the extended variant).  In
  that fallback path the firmware does *not* probe during the join
  — it strictly looks up the cached BSS by SSID+BSSID+chanspec —
  which is exactly the fragile mode we accidentally selected.

- **Failure code mapping.**  `BRCMF_E_STATUS_NO_NETWORKS = 3`
  (`fweh.h:122`) is the firmware's signal for "I have no cached BSS
  matching the supplied params" in the fallback path, *and* for
  "active probing on the supplied chanspec found nothing" in the
  extended `join` path.  Linux does not retry automatically; it
  reports failure to cfg80211 and lets wpa_supplicant decide.

**Implication for our driver.**  Use the `join` IOVAR with
`scan_le` set to all sentinel `-1` values (use firmware defaults
for `nprobes` / `active_time` / `passive_time` / `home_time`).
Only fall back to `WLC_SET_SSID` if the firmware rejects `join`
with `BCME_BADARG`, which would indicate that this revision lacks
the extended variant — and in that case we'd also need to pre-scan
on the target channel immediately before the connect.

> **CORRECTION (2026-05-25):** the above paragraph turned out to be
> wrong for firmware 7.45.265 on CYW43455.  The extended `join`
> IOVAR is **rejected with BCME_NOTUP (-14) on every payload format
> we tried**, including the exact Linux wire format
> (no prefix when bsscfgidx=0 — see §16.5).  The working path on
> this firmware is `WLC_SET_SSID` with all the *other*
> prerequisites met (preinit IOVARs after WLC_UP, see §16.5).  See
> §16.6 for the full session log.

### 16.5 freebsd-brcmfmac alignment findings (2026-05-25)

The existing FreeBSD port at `/Users/aphor/src/freebsd-brcmfmac.git`
already solved most of the problems documented above.  Reading their
source produced the following corrections to earlier assumptions:

#### 16.5.1 `brcmf_fil_bsscfg_data_set` wire format

The Linux helper `brcmf_fil_bsscfg_data_set(ifp, "name", data, len)`
does **NOT** unconditionally prepend a 4-byte bsscfgidx.  Source
(`brcmfmac/fwil.c:259`, also `freebsd-brcmfmac/src/fwil.c:89`):

```c
if (bsscfgidx == 0)
    return brcmf_create_iovar(name, data, datalen, buf, buflen);
/* non-zero: "bsscfg:" + name + NUL + bsscfgidx_le32 + data */
```

For the primary BSS (idx=0, our only case as a single-VAP STA),
the wire format is a **plain iovar** with no prefix bytes at all.
For non-zero bsscfgidx (P2P / AP secondary interfaces), the prefix
is the literal string `"bsscfg:"` (7 bytes, no NUL) followed by
`name + NUL`, followed by the 4-byte LE bsscfgidx, followed by data.

Our earlier session-time `cyw_fil_bsscfg_data_set` implementation
unconditionally prepended 4 zero bytes, which was wrong for both
cases.  It was producing the firmware-required
`[bsscfgidx_le32][enable_le32]` 8-byte payload **by accident** for
the `"bss"` iovar, while silently corrupting payloads for every
other bsscfg call.  Fixed in commit `109c9ae`.

#### 16.5.2 `"wpaie"` is harmful on CYW43455

`freebsd-brcmfmac/src/cfg.c:597-601` and `cfg.c:1018-1022`:

> CYW43455 (7.45.x) returns UNSUPPORTED for wpaie, and the failed
> iovar can taint firmware WPA state ... BCM4350 (7.35.x) accepts
> it but sending the RSN IE corrupts firmware WPA state, causing
> AUTH frames to fail with NO_ACK ... **Do not enable on any chip.**

We tried `wpaie` with a hand-built 22-byte RSN IE for WPA2-PSK-CCMP
and the call returned 0 — but per the comment above, "returns 0"
is exactly the silent-poisoning failure mode they warn about.
Removed in commit `109c9ae`.

#### 16.5.3 Preinit IOVARs after WLC_UP are required, not optional

The list of IOVARs in `freebsd-brcmfmac/src/cfg.c:1120-1153` looks
like "tuning" but is in fact a set of **firmware-side preconditions**
for the join state machine.  Without them, on CYW43455 firmware
7.45.265 the `WLC_SET_SSID` command returns success but the
firmware never transmits an authentication frame — `E_AUTH
status=2 (TIMEOUT)` repeats `assoc_retry_max` times and the
firmware reports `E_SET_SSID status=1 (FAIL)`.

The minimum set, in order, after `WLC_UP` + 200 ms settle +
`WLC_SET_INFRA=1`:

- `WLC_SET_SCAN_CHANNEL_TIME = 40` (cmd 185)
- `WLC_SET_SCAN_UNASSOC_TIME = 40` (cmd 187)
- `WLC_SET_SCAN_PASSIVE_TIME = 120` (cmd 258)
- `bcn_timeout = 4` (iovar)
- `assoc_retry_max = 3` (iovar)
- `WLC_SET_FAKEFRAG = 1` (cmd 219; frameburst)
- `txbf = 1` (iovar; BCME_UNSUPPORTED on this FW, set anyway)

Implemented in commit `eef713f`.

#### 16.5.4 `"bss"` IOVAR has a struct payload, not an int

```c
struct { __le32 bsscfgidx; __le32 enable; } bss_enable;
bss_enable.bsscfgidx = cpu_to_le32(ifp->bsscfgidx);
bss_enable.enable = cpu_to_le32(1);
brcmf_fil_iovar_data_set(ifp, "bss", &bss_enable, sizeof(bss_enable));
```

8 bytes total.  Calling `bsscfg_int_set(sc, "bss", 1)` produces a
4-byte payload that the firmware rejects with BCME_NOTUP.  In any
case, `freebsd-brcmfmac` does **not** issue `bss enable` on the
primary STA bsscfg — only on P2P/AP secondary bsscfgs.  For STA-only
we omit the call entirely.  Removed in commit `eef713f`.

#### 16.5.5 `"country"` IOVAR + NVRAM regdomain

The `"country"` IOVAR returns BCME_BADARG on FW 7.45.265 for the
SET form with both `rev=0` and `rev=-1`, but the GET form returns
the active regdomain cleanly.  Confirmed (commit `6ba08ff`) via:

```
pre-join: cur_chan=8 chanspec=0x1008 qtxpower=0x7f
          country="US"/"US" rev=0 (get_country=0)
```

`qtxpower=0x7f` (127 quarter-dBm = ~32 dBm EIRP, the US 2.4 GHz
maximum) and `country="US"` come from the NVRAM file
(`brcmfmac43455-sdio.txt`) which already ships with `ccode=US`.  The
firmware does not need (and does not accept) a runtime country
override on this build.

This refutes the earlier hypothesis that regdomain-restricted TX was
causing `E_AUTH status=2 (TIMEOUT)`.  TX is at maximum permitted
power on the correct regdomain.  The TIMEOUT was caused by the
missing preinit IOVARs (§16.5.3), not by regdomain.

### 16.6 BCME_NOTUP from `"join"` on FW 7.45.265 — full investigation log

Across the 2026-05-25 session every Linux-side payload-format
hypothesis for `"join"` returning BCME_NOTUP was tested and ruled
out, in this order:

| Hypothesis | Action | Outcome |
|---|---|---|
| Missing 4-byte bsscfgidx prefix on `"join"` payload | Added | Still BCME_NOTUP |
| Missing bsscfg scoping on `auth`/`wsec`/`wpa_auth` | Switched to bsscfg helpers | Still BCME_NOTUP |
| Wrong `scan_le` initialization (scan_type, dwell times) | Matched Linux verbatim: scan_type=-1, 320/400/16 | Still BCME_NOTUP |
| BSSCFG not enabled at firmware level | `"bss" enable=1` via correct 8-byte payload | Enable returns 0, join still NOTUP |
| Missing RSN IE for AssocReq | `"wpaie"` with hand-built 22-byte RSN | wpaie returns 0, join still NOTUP, +state poisoning |
| WLC core not actually UP | Re-issued `WLC_UP` immediately before join | WLC_UP=0, join still NOTUP |
| Wire format prefix bug | Pass-through to plain iovar for idx=0 | Format now Linux-correct, join still NOTUP |
| Regdomain restricting TX | Verified via GET: US, qtxpower=0x7f | Refuted |

**Conclusion.**  The bsscfg-scoped `"join"` IOVAR variant is
selectively broken in firmware 7.45.265 on the CYW43455 / Aug 2023
Cypress drop.  The synchronous BCME_NOTUP return is a stub in the
firmware-side iovar dispatcher.  No payload alignment will fix this
on this firmware.

The working association path is **WLC_SET_SSID** (the legacy `join`)
with all of §16.5.3's preinit IOVARs in place AND a target BSS that
is still in the firmware's BSS cache.  freebsd-brcmfmac handles the
identical situation by trying `"join"` first and unconditionally
falling back to `WLC_SET_SSID` on any error (`src/cfg.c:288-407`).
Our driver does the same in `cyw43455_cfg.c` as of commit `98ff749`.

### 16.7 The session's final remaining symptom: EAPOL flow

After all the above corrections, the test now reaches association:

```
wlan0: Associated with 7e:d2:94:3b:fb:3f
wlan0: Authentication with 7e:d2:94:3b:fb:3f timed out.
[driver] E_DISASSOC reason=8 after link=1
```

`E_DISASSOC reason=8` after `E_LINK link=1` is the original
WPA2-4-way-handshake-timeout symptom that motivated the entire
session's `rx_eapol_frames` counter work (see plan
`floofy-whistling-scott.md` Step 4).  `rx_eapol_frames=0` at the
end of every test confirms the firmware is not delivering EAPOL M1
on SDPCM channel 2 the way we expect.  This is the next live
investigation; see §16.4 commentary above for the original analysis
and `cyw43455_sdpcm.c:104-137` for the channel-2 dispatch path.

---

### 16.8 FWSUP probe — firmware is host-supplicant mode

#### What we set out to establish

Linux brcmfmac detects firmware-internal WPA supplicant (FWSUP) at
attach via `brcmf_feat_iovar_int_get(ifp, BRCMF_FEAT_FWSUP,
"sup_wpa")` (`feature.c:349`).  If supported, it enables FWSUP per
connect with `brcmf_fil_iovar_int_set(ifp, "sup_wpa", 1)`
(`cfg80211.c:2495-2502`) and the firmware then runs the full 4-way
handshake itself, signalling completion via
`BRCMF_E_PSK_SUP status=6` (`BRCMF_E_STATUS_FWSUP_COMPLETED`,
`fweh.h:136-144`).  In host-supplicant mode, EAPOL M1/M3 arrive on
SDPCM channel 2 and wpa_supplicant generates M2/M4 via TX.

The prior §16.3 claim that this firmware was FWSUP rested on a
single observation that "`sup_wpa` returns `BCME_BADARG`", with no
recorded test methodology.  This section records the actual probe.

#### How we tested

`cyw43455.c` commit `3c4ae9e` added a one-shot probe behind
`hw.cyw43455.probe_fwsup=1` (loader tunable).  Commit `ebb9eba`
moved it from attach into `cyw_parent()` immediately after WLC_UP
+ 200 ms settle (the prior attach-time probe returned `BCME_NOTUP`
because the firmware was DOWN, mirroring `'pm'` IOVAR's behaviour).

Run sequence:

```
sudo kenv hw.cyw43455.probe_fwsup=1
sudo kldload cyw43455
sudo ifconfig wlan0 create wlandev cyw434550 && sudo ifconfig wlan0 up
```

#### What we observed

```
cyw434550: cyw_parent: WLC_UP ok
cyw434550: cyw_parent: pause done
cyw434550: probe_fwsup: begin (Linux FWSUP detector)
cyw434550: cyw_fil: firmware rejected cmd 262 'sup_wpa' status 0xffffffe9
cyw434550: probe_fwsup: GET sup_wpa returned 5 value=0xdeadbeef
cyw434550: cyw_fil: firmware rejected cmd 263 'sup_wpa' status 0xffffffe9
cyw434550: probe_fwsup: SET sup_wpa=1 returned 5
cyw434550: cyw_fil: firmware rejected cmd 263 'sup_wpa' status 0xffffffe9
cyw434550: probe_fwsup: SET sup_wpa=0 returned 5 (restore)
cyw434550: probe_fwsup: end
```

The firmware status code `0xffffffe9` is `-23` in two's complement.
Cross-referenced against brcmfmac `BCME_*` codes:

| Code | Meaning |
|---|---|
| -2  | BCME_BADARG (what §16.3 claimed) |
| -23 | **BCME_UNSUPPORTED** (what the firmware actually returns) |

The mapping to host errno 5 (`EIO`) is done by `cyw_fil_*`.  The
underlying firmware return is `BCME_UNSUPPORTED`, not `BCME_BADARG`.

#### What it means

FW 7.45.265 on the CYW43455 **does not implement the `sup_wpa`
iovar at all**.  Linux's feature detector would record this as
`BRCMF_FEAT_FWSUP=false` and the entire FWSUP path would be skipped.

That puts us in Linux's **host-supplicant fallback**, a conclusion
now confirmed by three independent sources:

| Source | Evidence |
|---|---|
| Linux brcmfmac code | `feature.c:349` FWSUP detection; `cfg80211.c:2495-2507` only enables `sup_wpa=1` and calls `set_pmk` when FWSUP is present |
| Direct firmware probe (this driver, commits `3c4ae9e`/`ebb9eba`) | `sup_wpa` GET, SET(1), SET(0) all return `BCME_UNSUPPORTED (-23)` even after `WLC_UP` |
| Infineon Wi-Fi Software User Guide (AN232689, rev *D, 2026-01-19) | §4.1 p.23: "wpa_supplicant ... implements the key negotiation with an authenticator"; §4.3.3.2 p.34: "**For WPAx security, use wpa_supplicant instead**" (of `iw`/driver-only); Figure 1 p.4 places wpa_supplicant above cfg80211 → FMAC → firmware with no firmware-supplicant box |

Consequences for the driver:

1. `wpa_supplicant` on the host owns the 4-way handshake.  The
   vendor User Guide §4.1.4.3 documents the exact `wpa_cli` sequence
   (`add_network`, `set_network 0 psk "..."`, `proto WPA2`,
   `key_mgmt WPA-PSK`, `pairwise CCMP`) — passphrase goes to
   `wpa_supplicant`, never to the driver.
2. The driver must not call `WLC_SET_WSEC_PMK` — Linux only does so
   inside `if (use_fwsup != BRCMF_PROFILE_FWSUP_NONE)`
   (`cfg80211.c:2495-2507`).  In host mode the PMK lives in
   wpa_supplicant; the firmware never sees it.
3. EAPOL M1/M3 frames must reach the host on SDPCM channel 2 and be
   delivered to the VAP's ifnet via `if_input` so wpa_supplicant's
   BPF socket on wlan0 can pick them up.
4. Keys derived by wpa_supplicant must be installed via
   `IEEE80211_IOC_SETKEY` → driver key-management op
   (`ic_node_setkey` or `iv_key_set`) → `WLC_SET_KEY` IOVAR.
   At the time of the probe our driver had no key-management op
   wired — without it the 4-way handshake completes in
   wpa_supplicant but the firmware never receives the PTK/GTK.
   **This is now resolved — see §16.9.**  `cyw_key_set` installs
   both keys via `WLC_SET_KEY` and unicast + multicast data flow
   works.

Items (2) and (3) were already wrong in this driver before the
probe: we always called `WLC_SET_WSEC_PMK` (in passphrase mode, on
top of being unsupported in host mode), and we delivered channel-2
frames via `ieee80211_input_all` (which silently discards ethernet
frames).  Commit `429c66f` fixed (3) by switching to `if_input`.
This commit fixes (2).  Item (4) is the next session's work.

#### Related SDIO tuning hints

The User Guide §3.3.5 p.19 documents these `brcmfmac` Linux module
parameters for "maximum throughput" on the SDIO interface:

```
fcmode=2          # firmware-controlled flow control
sdio_in_isr=1     # RX processing inside the ISR
sdio_rxf_thread=1 # dedicated RX thread for forwarded frames
```

None of these are switches we have today (our RX is a 50 ms polled
callout, not IRQ-driven), but `fcmode=2` is interesting: it implies
the firmware's flow-control machinery is intended to drive RX
throttling end-to-end, which we can revisit when we move from
polled to F2 IRQ (deferred to TODO.md Milestone 3, see plan
`floofy-whistling-scott.md` §"When to revisit IRQ").

#### What it does not explain

The current `E_AUTH status=2` (TIMEOUT) symptom occurs *before*
any of the above matters.  Auth frame exchange is firmware-internal
to firmware-external; wpa_supplicant and host PMK state are not
relevant until after association completes.  The `E_AUTH TIMEOUT`
investigation is separate from the FWSUP-vs-host question and
deferred until either the test AP starts responding to 2.4 GHz auth
frames or we test the 5 GHz path with corrected D11AC chanspec
encoding.

---

### 16.9 Resolution — WPA2-PSK unicast + multicast data flow works (5 GHz)

This section supersedes the open items left at the end of §16.8.
Item (4) — "our driver has no key-management op wired" — is now
resolved, and item §16.7's EAPOL/unicast-loss symptom does **not**
reproduce on a clean boot.

#### Final architecture (host-supplicant, as §16.8 predicted)

1. `wpa_supplicant` on the host owns the 4-way handshake.
   Association reaches `wpa_state=COMPLETED`,
   `pairwise_cipher=CCMP`, `key_mgmt=WPA2-PSK`.
2. EAPOL M1/M3 arrive on SDPCM channel 2 and are delivered to the
   VAP ifnet via `if_input` (commit `429c66f`); wpa_supplicant TXes
   M2/M4.
3. Keys derived by wpa_supplicant are installed through
   `IEEE80211_IOC_SETKEY` → `cyw_key_set` → `WLC_SET_KEY`
   (`wsec_key` iovar).  This is the key-management op §16.8 item (4)
   said was missing; it is now present and proven correct.

#### Key-install correctness (proven byte-for-byte vs. Linux)

`cyw_key_set` (`cyw43455_cfg.c`) builds `struct cyw_wsec_key`
matching `brcmf_wsec_key_le` exactly:

| Field | PTK (pairwise) | GTK (group) |
|---|---|---|
| index | 0 | 1 |
| len | 16 (CCMP) | 16 |
| algo | 4 (`AES_CCM`) | 4 |
| flags | 0 | 0 |
| ea | AP MAC | zero |
| iv_initialized | 0 | **1** (commit `63ea24e`) |
| rxiv | 0 | from `wk_keyrsc` |

`wsec_key` is **write-only** on FW 7.45.265 (GET →
`BCME_UNSUPPORTED`); Linux `brcmf_cfg80211_get_key` never reads it
back either, so no read-back diagnosis is possible — this is normal.

The lock dance in `cyw_key_set` drops `IEEE80211_LOCK` /
`IEEE80211_NODE_LOCK` around the sleeping `wsec_key` iovar and
reacquires after.

#### TX framing correctness (proven byte-for-byte vs. brcmfmac)

A toggleable header dump (`hw.cyw43455.tx_hdr_debug=1`, commit
`e2bc5a5`) confirmed unicast and multicast TX frames carry identical
SDPCM + BDC headers — only the seq number and payload DA differ:

```
UCAST  len=88  SDPCM=5800 a7ff 3d 02 00 0c 0000 0000  BDC=20 00 00 00
MCAST  len=88  SDPCM=5800 a7ff 40 02 00 0c 0000 0000  BDC=20 00 00 00
```

`BDC=20 00 00 00` (`proto_ver=2<<4`, priority 0, ifidx 0,
data_offset 0) is identical to `freebsd-brcmfmac/src/sdpcm.c:643-646`.

#### Test results — clean cold boot (e2bc5a5)

| Test | UCAST → gateway | MCAST → ff02::1 |
|---|---|---|
| Driver loaded, fresh boot | 3 tx / **3 rx, 0% loss** | 2 rx +17 dups, 0% loss |
| Driver unloaded (control) | `wlan0` gone, address unresolvable | same |

The unload control confirms the working traffic genuinely traversed
the cyw43455 interface (no stray wired/loopback path).

#### Root cause of the earlier "100% unicast loss"

The unicast-loss symptom that drove §16.7 and several prior sessions
**correlated with a dirty rapid-reload SDIO state**, not driver
logic.  Rapid `kldunload`/`kldload` cycling intermittently produced
`unexpected chip ID 0xffff (expected 0x4345)` → `SDIO attach failed:
6`, leaving the firmware/SDIO in a partially-initialized state.  A
clean cold boot (or an adequately-settled reload) yields working
bidirectional unicast every time.

**Operational note:** when reloading the module, allow the SDIO core
to settle (a few seconds, or prefer a cold boot) before re-testing.
A `0xffff` chip-ID read at attach is the signature of reloading too
fast and should be treated as "retry from clean", not a code defect.

---

### 16.10 2.4 GHz WPA2-PSK association works

Prior sessions treated 2.4 GHz `E_AUTH status=2` (TIMEOUT) as a
terminal, possibly band-specific failure (§16.8 "What it does not
explain").  A clean re-test disproves that: **2.4 GHz WPA2-PSK
association succeeds**, and the initial TIMEOUT is non-fatal — the
firmware retries and clears it on its own.

#### Test environment

The test AP `localnet` beacons multiple co-located BSSes:

| BSSID | Band / chan | chanspec | SSID beaconed |
|---|---|---|---|
| `8c:3b:ad:1b:87:71` | 5 GHz / 48 | `0xe32a` | `localnet` |
| `8c:3b:ad:1b:87:72` | 5 GHz / 157 | `0xe29b` | (hidden) |
| `92:3b:ad:1b:87:6f` | 2.4 GHz / 8 | `0x1008` | `localnet` (revealed by directed probe) |
| `96:3b:ad:1b:87:6f` | 2.4 GHz / 8 | `0x1008` | (hidden — a *different* VAP, not localnet) |

#### What we observed

Targeting the correct 2.4 GHz BSSID `92:3b:ad:1b:87:6f` (chan 8):

```
E_AUTH status=2  (TIMEOUT, first attempt)
E_AUTH status=0  (success on retry)
E_ASSOC status=0
E_LINK link=1
E_SET_SSID status=0
→ wpa_state=COMPLETED, pairwise=CCMP
```

Result: stable `COMPLETED`, CCMP, and multicast data flows
(`ping6 ff02::1`: 3/3, 0% loss, ~0.06 ms RTT).  The `chanspec=0x1008`
(chan 8) decoded and joined correctly.

#### A BSS-selection footgun (not a 2.4 GHz defect)

A first attempt targeting `96:3b:ad:1b:87:6f` (a *hidden* VAP whose
beacon SSID is blank, **not** localnet) failed differently: the
driver logged `AUTH: WLC_SET_SSID fallback (chan=48)` and joined the
**5 GHz** localnet (`8c:…:71`) instead, then was kicked off with
`E_DISASSOC reason=8`.

Root cause — **not** stale BSSID state in the driver.  The driver
does not independently lock or clear a BSSID; it joins whatever
net80211 places in `vap->iv_bss->ni_bssid` (`cyw43455_cfg.c:119-125`).
When the requested `bssid=` belongs to a BSS whose beaconed SSID does
not match the configured `ssid="localnet"`, net80211 selects the best
*other* BSS that does match the SSID — here the 5 GHz AP — and the
driver faithfully joins it.  The band switch is net80211 BSS
selection given a self-inconsistent `(bssid, ssid)` pair, not a
driver bug.

**Operational rule:** a `bssid=` lock in `wpa_supplicant.conf` must
name a BSS whose beaconed SSID matches `ssid=`.  Locking to a hidden
sibling VAP will silently land on a different-band AP.

#### Status of the old "E_AUTH TIMEOUT" symptom

The §16.8 deferral ("the `E_AUTH status=2` TIMEOUT investigation is
separate … deferred until the AP responds") is now closed: the
TIMEOUT is a benign first-attempt artifact that the firmware retries
through to `E_AUTH status=0`.  No driver change was required.

---

## 15. Reading list, in priority order

When stuck, read these — in this order — before guessing:

1. `../raspbian_linux.git/drivers/net/wireless/broadcom/brcm80211/brcmfmac/sdio.c`
   sections: ~lines 200–280 (constants), 700–800 (KSO/HOSTINTMASK),
   1100–1170 (hostmail), 2580–2880 (DPC main loop), 3800–3900 (ISR),
   4400–4500 (probe), 4750–4800 (remove).
2. `../raspbian_linux.git/drivers/net/wireless/broadcom/brcm80211/brcmfmac/chip.c`
   sections: 1050–1100 (`brcmf_chip_disable_arm`), 1280–1320
   (`brcmf_chip_cr4_set_passive`), 1400–1480 (`brcmf_chip_sr_capable`),
   1500–1570 (`brcmf_chip_set_active`).
3. `../raspbian_linux.git/drivers/net/wireless/broadcom/brcm80211/brcmfmac/bcdc.c`
   — BCDC framing, especially `brcmf_proto_bcdc_msg`.
4. `../freebsd-brcmfmac.git/src/sdio.c` and `docs/03-sdio-reference.md`,
   `docs/12-sdio-retro.md` — a parallel FreeBSD attempt with a more
   complete RX path. Most useful for net80211 wiring; the SDIO
   bring-up has different bugs than ours.
5. `_reference/infineon-cyw43455-datasheet-en.pdf` —
   §1 chip block diagram; §2.4 SDIO host interface; §3 power/clocks;
   §4.3 sleep modes (KSO); §6 register summary. The datasheet is
   silent on SDPCM/BCDC framing — those are software conventions
   shared across the brcmfmac chip family.
